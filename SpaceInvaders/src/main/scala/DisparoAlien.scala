class DisparoAlien(coordX : Int, coordY : Int) {
  var x: Int = coordX;
  var y: Int = coordY;

  /*
* TODO: Desenhar disparo, movimentar disparo, verificar colisão com a nave.
*       Verificar colisão entre disparos (nave x alien).
*/
  def getX: Int = {
    return this.x;
  }
  def getY: Int = {
    return this.y;
  }
}